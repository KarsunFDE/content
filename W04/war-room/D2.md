---
week: W04
day: Tue
mode: workshop
title: "Spec-driven dev workshop + Brownfield Modernization Planning (post-Checkpoint 2)"
duration_minutes: 180
last_verified: 2026-05-26
audience: cohort
visibility: public
content_type: cohort-brief
---

# W4 Tue War-Room — Spec-driven dev workshop (post-Checkpoint 2)

> **Mode: Workshop.** Checkpoint 2 exam runs 09:00–10:30 in-person (per D-061). War-room starts **after the exam**, 10:30 onward. Walk in fresh — the workshop is structured to surface a discovery your pair must come to on its own.

## 1. What we're tackling today (and why)

Three weeks of merged PRs across `acquire-gov` + your pair-project repos all "passed CI." Today you walk those PRs back and confirm whether your CI actually checks what you think it checks. The workshop turns spec-driven dev from a Plan-Day-only artifact into a daily discipline — the cohort has practised the §0 retro 3× by now (W2/W3/W4 Mon), but a spec only counts if every PR ships against it. Afternoon: API-modernization warm-up (debt item 8 — frontend bypasses the gateway) and Thu OpenRewrite hop pre-flight on both `acquire-gov` and your pair-project repo.

## 2. What to know walking in

- Checkpoint 2 exam 09:00–10:30 (per D-061 — in-person, 90 min). War-room starts 10:30. Audit walkthroughs async via Zoom this week. `pre-session/2-Tuesday/1-DailyTopicOverview.md` read.
- Pre-flight the workshop will land on: Java 17 toolchain (container-first per D2 reframe — runs in container, no host JDK pinning), `mvn rewrite:dryRun` baseline captured, rescue branch named per stage.
- 12 baseline brownfield-debt items in `training-project/README.md` — re-read items 8 + 12 specifically. The OIG Findings Tracker (evaluation-service `POST /api/findings`) is real per `feature-inventory-target.md` line 197 — findings get opened against systems, including potentially your own.
- Codex Full strictness lands Thu — today's PRs are the last under Near-full. Use the easier bar to surface what Full will flag.

## 3. EOD deliverable (17:00 Tue)

1. **PR in `acquire-gov`** that addresses whatever the workshop surfaces — with a linked finding (`POST /api/findings`, format per `feature-inventory-target.md` Finding entity line 237: `finding_type`, `severity`, `evidence_requests[]`, `remediation_due`).
2. **Debt item 8 fix** — frontend hardcoded URL routed through `api-gateway`. PR open, codex review queued.
3. **OpenRewrite hop pre-flight checklist** committed to `planning/W04/03-thu-hop-preflight.md` — toolchain check, `dryRun` baseline output, rescue-branch naming convention, `known-failures.md` skeleton.
4. **Risk Register updated** in `planning/W04/risk-register.md` with whatever today surfaces as risks for Thu's execution and Fri's exec brief.
5. **Pair-project parallel work:** same workshop pattern run against your pair-project repo; per-pair-unique debt may differ per D-059.

## 4. Reference

- `weeks/W04/{PLAN,README}.md` · `training-project/README.md` (debt 8+12) · `training-project/feature-inventory-target.md` (Finding entity, OIG Findings Tracker)
- `research/spring-boot-3-x-20260525.md` · `pipeline/DECISIONS.md` — D-034 (codex strictness), D-059 (per-pair-unique debt), D-061 (audit format)
