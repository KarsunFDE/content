---
week: W04
day: Wed
mode: incident
title: "AI Security Day — OWASP LLM Top 10:2025 + HITL #6 + dedicated /web-research slot"
duration_minutes: 180
last_verified: 2026-05-26
audience: cohort
visibility: public
content_type: cohort-brief
---

# W4 Wed War-Room — AI Security Day + HITL #6

> **Mode: Incident.** Wednesday is the AI Security depth day. A sibling-agency OIG finding lands at 09:00; your pair walks `acquire-gov`'s prompt-injection-via-stored-content surfaces against OWASP LLM01:2025. The afternoon exercises HITL #6 as the **LLM06 Excessive Agency** authority-boundary artifact.

## 1. What we're tackling today (and why)

An OIG audit at a sibling agency just flagged an AI drafter (same shape as `acquire-gov`'s `/draft-amendment`) for emitting fabricated FAR clauses into a published Q&A response after accepting a prompt-injected vendor question. Today the Karsun Security Engineering Lead asks you to (1) confirm the same shape exists in `acquire-gov`'s 4 stored-content surfaces, (2) build a working JWT-skip exploit so you know how bad debt item 1 really is, and (3) deliver an LLM06 authority-boundary table covering every endpoint in the ai-orchestrator. **HITL #6 is the authority-boundary table.** Federal language for "what authority does the AI exercise + where does a human gate it + what's the rollback" — that table IS the touchpoint. Density at the upper cap (8 topics) — if anything carries to Thu AM, the JWT exploit demo defers, not the authority table.

## 2. What to know walking in

- `pre-session/D3.md` read. OWASP LLM Top 10 **2025 (v2.0)** is current per `research/owasp-llm-top-10-20260522.md`. LLM07 = **System Prompt Leakage**; LLM08 = **Vector/Embedding Weaknesses**. The 2023 list (LLM07 = "Insecure Plugin Design") is stale — do not cite it.
- The 4 stored-content surfaces (per `feature-inventory-target.md` line 352): `POST /api/solicitations` description, `POST /api/solicitations/{id}/qa`, `POST /api/awards/{id}/debrief-request`, `POST /api/contracts/{id}/cpars/{cparId}/rebuttal` — all feed `/draft-amendment` or `/answer-qa`.
- Debt item 1 (JWT-skip on `/api/public/**`) + debt item 9 (no input sanitization on the 4 surfaces) are today's primary targets. Items 10 + 11 get conceptual treatment.
- JWT-skip exploit demo stays **instructor-owned** — `weeks/W04/wed-jwt-skip-exploit.md`, not committed to `acquire-gov` main. Don't push it. Dedicated `/web-research` slot per D-040 doubles up — same block researches W04-SA-1/2/3.

## 3. EOD deliverable (17:00 Wed)

In `planning/W04/security/`:
1. **Stored-content injection map** — `01-stored-content-injection-map.md`. 4 surfaces × OWASP LLM01:2025 vector. At least 2 end-to-end proof-of-concept injections demonstrated.
2. **JWT-skip exploit** — working exploit in `weeks/W04/wed-jwt-skip-exploit.md` (instructor-owned location). LLM07 + LLM08 framing.
3. **Item 9 prompt-injection fix PR** — Pydantic v2 input validators + output sanitisation on the 4 surfaces. Codex Full strictness on the PR.
4. **HITL #6 authority-boundary table** — `03-hitl-authority-boundary.md`. 8 ai-orchestrator endpoints × 4 columns (caller, AI authority, human gate, rollback). **This IS HITL #6.**
5. **W04-SA-1/2/3 research notes** committed to `planning/W04/research/` from the dedicated /web-research block.

## 4. Reference

- `weeks/W04/{PLAN,README}.md` · `research/owasp-llm-top-10-20260522.md` · `training-project/feature-inventory-target.md` (4 surfaces, 8 ai-orch endpoints) · `training-project/README.md` (debt 1/9/10/11)
- `pipeline/DECISIONS.md` — D-040 (Wed research slot), D-043/D-044 (HITL thread), D-046 (web-research-only)
