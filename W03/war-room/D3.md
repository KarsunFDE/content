---
week: W03
day: Wed
mode: incident
title: "D3 Wed — Multi-agent evaluator flow + HITL #4 supervisor soft-interrupt"
duration_minutes: 180
last_verified: 2026-05-26
audience: cohort
visibility: public
content_type: cohort-brief
---

# W3 D3 Wed War-Room — What you're tackling today

> **Incident — pivot day.** Mon–Tue's intake-triage hands off into Wed–Fri's harder shape: **evaluator-agent → consensus-agent → SSA-review-agent.** HITL #4 (multi-agent handoffs) lands today. War-room 09:00–12:00; dedicated scenario-research slot PM (per D-040).

## What we're tackling today + why

Three concurrent evaluations kick off — RFP-2026-GSA-1184 (cloud), 1199 (cyber), 1204 (mobile). Six evaluators each. The SSA wants something new: **visibility into the supervisor's decisions before consensus completes.** Not at the SSDD boundary (that's tomorrow's hard gate) — earlier. She wants to see what the supervisor is *about to delegate*, approve or override, before it fires.

That's HITL #4 — a **soft interrupt at the supervisor → worker handoff.** Different shape from HITL #3 (ADR-level boundaries) and HITL #5 (Thu's hard interrupt + FAR 15.308 audit). Don't pre-collapse them.

Today's load surfaces two brownfield-debt items: **Item 2 (audit-log race)** — 3 evals × 6 evaluators × ~4 events = ~72 audit rows fanning into one Postgres write. **Item 6 (correlation IDs)** — without a threaded ID per `evaluation_id`, you cannot reconstruct the multi-agent trace. Both combine into the W4 Mid-Sprint Surprise — you see the precursor today.

## What to know walking in

- `pre-session/3-Wednesday/1-DailyTopicOverview.md` read — supervisor-worker + multi-agent anti-patterns + KG/CG thinking.
- Tue's PR merged; LangSmith traces available to replay.
- LangGraph `interrupt_before` reviewed; PostgresSaver concept clear (light wire today, full wire Thu).
- FAR 15.308 sets up Thu's hard-gate; today's soft-gate is the *option* the SSA chose to expose.

## EOD deliverable (Wed 17:00) — convergence by 12:00, scenario research PM

1. **Supervisor-worker scaffolding** — supervisor + one worker type (evaluator-agent) end-to-end against `EvaluationState`.
2. **`interrupt_before=["supervisor_decide_next_worker"]`** configured; soft interrupt fires; resume via `Command(resume=...)` works in a unit test.
3. **Single audit-writer pattern** — one utility emits AuditEvents from `EvaluationState.audit_correlation_id`. No fan-out.
4. **LangSmith trace** showing supervisor decision → pause → resume → worker → result loop.
5. **Three SA prompts opened** — SA-1 (single vs multi for intake-triage), SA-2 (LangGraph vs LangChain vs hand-rolled), SA-3 (Neo4j vs Postgres recursive CTE vs NetworkX). Full ADRs due Thu W4.
6. **Pair-retro note** on Item 3 (no circuit breaker eval→sol) — fix lands W5; name it today.

## Reference

- Source: `weeks/W03/PLAN.md` Wed row · `weeks/W03/war-room/Wed.md` · `pre-session/3-Wednesday/1-DailyTopicOverview.md`
- Research: `research/langchain-v1-20260522.md`
- Scenarios: `scenarios/W03-SA-1.md`, `W03-SA-2.md`, `W03-SA-3.md`
- FAR 15.308 (SSA decision authority — cannot delegate)
- Tomorrow: `pre-session/4-Thursday/1-DailyTopicOverview.md` (state schema + LangGraph HITL deep-dive) — HITL #5 technical anchor
