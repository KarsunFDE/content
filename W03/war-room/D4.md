---
week: W03
day: Thu
mode: incident
title: "D4 Thu — SSA SSDD hard-gate + 18-hour resume + HITL #5 LangGraph deep-dive"
duration_minutes: 180
last_verified: 2026-05-26
audience: cohort
visibility: public
content_type: cohort-brief
---

# W3 D4 Thu War-Room — What you're tackling today

> **HITL #5 technical anchor.** Full evaluator → consensus → SSA-review LangGraph state machine ships by 17:00. Hard `interrupt_before` at the SSA boundary. PostgresSaver surviving a multi-hour gap. Audit rows citing FAR 15.308 on resume. Deepest LangGraph day of the programme.

## What we're tackling today + why

The SSA speaks: *"Your supervisor agent fanned out RFP-2026-GSA-1184 to four evaluators yesterday. Consensus completed at 23:00. It's now 17:00 — **18 hours later.** I have 15 minutes. When I click approve, is your system going to record what I approved, or is it going to regenerate the draft against today's RAG corpus because the FAR was amended this morning? Because if it regenerates, my approval isn't on the document I read."*

That's a FAR 15.308 violation question. The SSA's independent judgment cannot be delegated to a "fresh-context regeneration." By 17:00 the answer needs to be in code. Your `record_award` and `ssa_review_ssdd` nodes both become **hard interrupts**; the SSDD draft is snapshotted into graph state at consensus completion and never regenerated on resume. PostgresSaver makes the 18-hour gap survivable. Audit rows on resume cite the FAR clause that justifies the gate.

## What to know walking in

- `pre-session/D4.md` read — state schema + checkpointing + soft vs hard interrupts + cost/latency instrumentation.
- Wed's supervisor-worker scaffolding merged; soft interrupt working in unit test.
- FAR 15.308 + FAR 5.705 read carefully. Non-negotiable today.
- PostgresSaver tutorial skimmed — full wire against acquire-gov Postgres lands today.
- Item 2 (audit-log race) precursor lived Wed. Today: one audit-writer; correlation_id threaded everywhere.
- **Phase 1 Defense rubric** preview — what you ship today is most of what you defend tomorrow.

## EOD deliverable (Thu 17:00)

1. **Full `evaluator → consensus → ssa_review_ssdd → record_award` LangGraph state machine** wired + tested.
2. **Two hard `interrupt_before` nodes:** `ssa_review_ssdd` (FAR 15.308) + `record_award` (FAR 5.705).
3. **PostgresSaver** against acquire-gov Postgres; threads keyed by `f"{agency_id}:{evaluation_id}"` (Item 10).
4. **Audit row schema** with `far_citation` set on every HITL_HARD_RESUME ("15.308" SSA, "5.705" award).
5. **Restart test passing**: graph runs → hard-interrupts at SSA → FastAPI restarts → resumes via `Command(resume=...)` against the **same draft text** (not regenerated) → `record_award` runs → audit rows correct.
6. **LangSmith trace** captured + screenshotted — Defense evidence for Friday.
7. **Cost + latency instrumentation** in LangSmith metadata (token-per-node + latency-per-node).
8. **Codex Adversarial Review** on the PR (Near-full strictness). Expect findings on thread-id namespace, audit row completeness, resume-payload validation, and lack-of-timeout being *correct* (regulatory anti-pattern to auto-timeout an SSA decision).

## Reference

- Source: `weeks/W03/PLAN.md` Thu row · `weeks/W03/war-room/Thu.md` · `pre-session/D4.md`
- Research: `research/langchain-v1-20260522.md`
- FAR 15.308 (SSA non-delegation) · FAR 5.705 (award publication irreversible)
- Phase 1 Defense rubric (preview): `assessments/W03-Phase1-Defense-rubric.md`
- Tomorrow: `pre-session/D5.md` (Phase 1 Gate framing — short). **Friday is the Gate, not a war-room day.**
