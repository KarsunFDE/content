---
week: W02
day: D2
mode: incident
title: "Vendor Q&A drops in — FAR 15.208(a) vs DFARS 215.371-4 timing conflict"
duration_minutes: 180
last_verified: 2026-05-26
audience: cohort
visibility: public
content_type: cohort-brief
---

# W2 D2 Tue War-Room — What you're tackling today

> **Mode: Incident.** First incident-mode war-room of the cohort. The vendor's Q&A surfaces the clause-precedence problem from D-060 head-on — and `POST /answer-qa` has zero grounding today.

## What we're tackling today + why

A Contract Specialist on the GSA-FAS solicitation team caught a vendor question at 08:47: *"Section L.4 says proposals due in 30 days. FAR 15.208(a) says 30 calendar days. DFARS 215.371-4 references different timing for streamlined acquisitions over $7.5M. Which applies?"* It's a DoD-funded solicitation, so DFARS supplements FAR. The platform's `POST /answer-qa` would auto-publish a guess today. You have 2 hours before the vendor escalates.

You're framing the dual-corpus ingestion + dual-source citation pattern this morning, then implementing it this afternoon: FAR + DFARS into Atlas Vector Search with `corpus_source` metadata, a working `POST /rag/clause-search` hybrid, and the temporary HITL-escalate envelope on `POST /answer-qa` so nothing else ships unreviewed before Thu wires HITL #2 properly.

## What to know walking in

- `pre-session/2-Tuesday/1-DailyTopicOverview.md` read — chunking strategies + embedding model selection.
- Mon's Scenario Design Planning artifact + ADRs reviewed by your pair. The chunking + embedding + vector store decisions from yesterday are what you'll implement today.
- You know **DFARS supplements FAR** per 48 CFR §201.104 (Mon's framing; reinforced in pre-read).
- You know `ai-orchestrator` owns `POST /rag/clause-search` + `POST /answer-qa`; `solicitation-service` owns `ClauseLibraryEntry` + `GET /api/clauses/search`. Atlas is shared.
- D-060 Bedrock exception is live: real `InvokeModel` calls allowed from today. Token/cost accounting starts at request 1 — don't blow through quota on test runs without batching.
- Atlas Vector Search index definition syntax skimmed — you'll be declaring filter fields today.
- The `pinecone-client==5.0.0` debt (Item 7) is in scope this week. Whichever pair has a 30-min window rips it out + deletes the `/admin/config` lie.

## EOD deliverable (Tue EOD)

By end of day, four tickets land in the training repo:

1. **`feat(ai-orch)`** — RAG ingestion pipeline for FAR Part 15 + DFARS Part 215 into Atlas Vector Search with `corpus_source` + `supplements_far_clause_id` metadata.
2. **`feat(sol-svc)`** — `ClauseLibraryEntry` schema change: add `corpus_source` + `supplements_far_clause_id`.
3. **`feat(ai-orch)`** — `POST /rag/clause-search` hybrid retrieval (`$vectorSearch` + `$search` + `$rankFusion`) returning dual-source citations.
4. **`chore(ai-orch)`** — remove `pinecone-client` dep + `/admin/config` "pinecone available" line (debt Item 7).

Plus a **temporary HITL-escalate envelope** wired on `POST /answer-qa` (Thu hardens it). And an ADR draft on the dual-source citation envelope shape.

## Reference

- W2 PLAN: `weeks/W02/PLAN.md`
- DFARS supplements FAR (48 CFR §201.104) + DFARS 215.371-4 + FAR 15.208 — sources in `pre-session/2-Tuesday/1-DailyTopicOverview.md`
- FAR ambient context: `research/federal-ambient-far-20260525.md`
- eCFR Title 48 bulk download (FAR + DFARS source): source linked from `pre-session/2-Tuesday/1-DailyTopicOverview.md`
- Atlas Vector Search filter syntax: source linked from `pre-session/2-Tuesday/1-DailyTopicOverview.md`
- Bedrock Titan Embeddings v2 cost reference: source linked from `pre-session/2-Tuesday/1-DailyTopicOverview.md`
- Tomorrow's prep: read `pre-session/3-Wednesday/1-DailyTopicOverview.md` (advanced RAG patterns + multi-tenant retrieval); Wed afternoon is the **dedicated `/web-research` slot** per D-040 — bring your scenario-alternatives prompts loaded.
