---
week: W02
day: D3
mode: incident
title: "Ingestion regression dropped DFARS supplements + cross-tenant clause leak (Item 10)"
duration_minutes: 180
last_verified: 2026-05-26
audience: cohort
visibility: public
content_type: cohort-brief
---

# W2 D3 Wed War-Room — What you're tackling today

> **Mode: Incident.** Two issues land at 09:00 — one from Tuesday night's ingestion run, one from a sys_admin at a different agency. Together they force the multi-tenant boundary closure at the index level + the ingestion walker fix. **Afternoon is the dedicated `/web-research` slot per D-040 — no implementation in the PM.**

## What we're tackling today + why

The CO ran `POST /rag/clause-search?q=DFARS 215.371` against the new index. Empty result. Then ran `q=FAR 15.371` — got results. Tuesday's overnight re-ingestion silently dropped DFARS Parts ≥ 212 (folder-walker `max_depth=2` bug — DFARS supplements sit one folder deeper). She demos to the agency CIO tomorrow 09:00.

Ten minutes later, a sys_admin at DLA pinged: he searched `/api/clauses/search?q=trade-off` and saw a draft clause tagged `agency_id: GSA-FAS`. He shouldn't see that. The Atlas Vector Search index for the search-everything collection has **no filter field declared in the index definition**. Item 10 manifests on RAG retrieval, exactly as the dependency map predicted.

Both fixes ship before lunch. Then your afternoon is `/web-research` for the three scenario-alternatives prompts — no code.

## What to know walking in

- `pre-session/3-Wednesday/1-DailyTopicOverview.md` read — advanced RAG patterns (parent-child chunking, multi-query retrieval, contextual compression, reranking) + multi-tenant retrieval boundaries.
- You understand the difference between **pre-filter** and **post-filter** on Atlas Vector Search — pre-filter means declaring the filter field at index-definition time (requires index re-creation).
- You understand that the **clause library** is cross-tenant (every agency reads the same FAR) but **search-everything** is tenant-scoped (drafts, in-progress solicitations, audit events). Item 10 closure applies to the tenant-scoped collections.
- Tuesday's `pinecone-client` PR is merged. The `/admin/config` screen line is the tail — sol-svc owns it, not ai-orch.
- Atlas online index re-creation is the right answer (no downtime, ~minutes).
- Codex Adversarial Review at **Ramping strictness** lands today per D-034 — P1 architectural-drift findings now block PRs, not coach. Pre-v1.0 LangChain patterns (`Chain` class, `LLMChain.run()`) are blocking from today onward.
- Your three scenario-alternatives prompts (`W02-SA-1` vector store, `W02-SA-2` hybrid retrieval, `W02-SA-3` embedding model) are loaded and your `/web-research` queries planned.

## EOD deliverable

**Morning (before 12:00 lunch — hard cut):**

1. **`test(rag-eval)`** — first failing test: cross-tenant clause-search leak.
2. **`fix(atlas)`** — re-create `search_all` index with `agency_id` declared as pre-filter.
3. **`fix(ai-orch)`** — ingestion walker `max_depth` bug; re-ingest all DFARS Parts ≥ 212.
4. **`chore(sol-svc)`** — remove `/admin/config` "pinecone available" line (debt Item 7 tail).

CO's CIO demo Thu 09:00 unblocked. Marcus at DLA can self-verify by EOD.

**Afternoon (DEDICATED RESEARCH SLOT — NO IMPLEMENTATION):**

ADR drafts on each of `W02-SA-1` / `W02-SA-2` / `W02-SA-3`, with 2–3 candidate techs per scenario, each grounded in `/web-research` citations. **Drafts due Thu EOD.** Live Defense Fri 14:00.

## Reference

- W2 PLAN: `weeks/W02/PLAN.md`
- Atlas filter syntax (pre-filter mechanics): source linked from `pre-session/3-Wednesday/1-DailyTopicOverview.md`
- Multi-tenant retrieval framing: `pre-session/3-Wednesday/1-DailyTopicOverview.md` §multi-tenant
- D-040 dedicated research slot rule + scenario-alternatives templates: `templates/scenario-alternatives.md`
- Three W2 scenarios: `weeks/W02/scenarios/W02-SA-1.md`, `W02-SA-2.md`, `W02-SA-3.md`
- Tomorrow's prep: read `pre-session/4-Thursday/1-DailyTopicOverview.md` (RAG fallback patterns + **HITL #2 framing** — it's the second of seven programme HITL touchpoints).
