---
week: W02
day: D4
mode: incident
title: "Faithfulness regression — wrong-FAR-Part chunk auto-published as Section M factor (HITL #2)"
duration_minutes: 180
last_verified: 2026-05-26
audience: cohort
visibility: public
content_type: cohort-brief
---

# W2 D4 Thu War-Room — What you're tackling today

> **Mode: Incident. THIS IS WHERE HITL #2 LANDS** — the 2nd of 7 programme HITL touchpoints per D-043/D-044. Pre-session named the failure-mode taxonomy. Today you wire the `needs_human_review` envelope on `POST /answer-qa` + the CO review queue + the faithfulness-threshold gate. By 12:00 the platform stops auto-publishing unreviewed answers.

## What we're tackling today + why

The Source Selection Authority forwarded the CO a draft Q&A response the platform auto-published yesterday afternoon. Vendor asked about Section M evaluation factors for a cloud-modernization solicitation. The platform cited **FAR 47.305-2** as governing tradeoff weighting. FAR 47.305-2 is about **transportation packaging**. Embedding-model misfire: "evaluation" in the query embedded near "evaluation of packaging" in the FAR 47 chunk. Reranker promoted it. LLM dutifully composed a response from the wrong chunk. Faithfulness was high (~0.92) — the response WAS faithful to the wrong chunk. The check that would have caught it doesn't exist.

The SSA wants HITL on `POST /answer-qa` by EOD or she pulls the platform out of the pilot.

You're wiring the envelope pattern first (escape hatch ships before the underlying retrieval fix), then the LLM-as-judge faithfulness+relevance check at threshold 0.85 (Fri's eval harness will give you data to tune), then the CO review queue with 4-hour business-hours SLA. Same day you also ship the **Item 5 legacy chain migration** — `LLMChain.run()` out of Drafting Wizard + Amendment Editor + notification-copy generator per D-033 LangChain v1.0 posture. Codex Ramping strictness is now blocking on this, not coaching.

## What to know walking in

- `pre-session/D4.md` read — **especially** the three RAG failure modes (retrieval miss, faithfulness drop, wrong-chunk) and the HITL #2 envelope pattern.
- Yesterday's multi-tenant fix is live. Atlas index has `agency_id` pre-filter. Today is unrelated to Item 10.
- You know the difference between **faithfulness** (response matches retrieved chunks) and **relevance** (retrieved chunks match the query's expected scope). Yesterday's failure had high faithfulness AND low relevance. You need both checks.
- LLM-as-judge candidate is **Claude Haiku** on Bedrock (cheap, fast, real `InvokeModel` per D-060).
- Item 5 migration ships in the same PR or a paired PR — no more `LLMChain.run()` after today. Codex will block any PR that re-introduces it.
- Pair-Project teams also wire HITL #2 to their pair-domain Q&A endpoint (grants / FOIA / post-award) this afternoon.
- Drafter endpoints (`POST /draft-solicitation`, `POST /draft-amendment`) do NOT need the envelope — they're implicitly HITL-gated by the CS workflow that reviews drafts before publish. HITL #2 specifically scopes to the auto-publish path on `POST /answer-qa`.

## EOD deliverable (Thu EOD)

Four tickets land:

1. **`feat(ai-orch)`** — HITL #2 envelope on `POST /answer-qa`: `needs_human_review` status + faithfulness/relevance LLM-as-judge gate at threshold 0.85. Conservative default — Fri's harness will tune it.
2. **`feat(sol-svc)`** — CO review queue endpoint + minimal CS UI (review list, approve, edit-and-approve, reject-with-redraft).
3. **`refactor(ai-orch)`** — migrate `legacy_chain.py`: Drafting Wizard + Amendment Editor + notification-copy generator off `LLMChain.run()` to plain Python composition (D-033).
4. **`test(rag-eval)`** — yesterday's failing query (Section M → FAR 47.305-2) added to `qa.jsonl` as a regression fixture. Two fixtures now exist (Wed's cross-tenant leak + today's wrong-chunk) — Fri builds the full harness around them.

By 17:00 the SSA can verify: no auto-publish without faithfulness ≥ 0.85; review queue exists; yesterday's failing query is a regression test; audit log shows both auto-drafts and CO-reviewed states.

## Reference

- W2 PLAN: `weeks/W02/PLAN.md`
- HITL #2 framing (pre-read): `pre-session/D4.md`
- Three RAG failure modes (pre-read): `pre-session/D4.md` §failure-mode-taxonomy
- LangChain v1.0 HITL with interrupts (anchor for W3 Thu HITL #5, not today): source linked from `pre-session/D4.md`
- RAGAS faithfulness metric definition: source linked from `pre-session/D4.md`
- OWASP LLM06 Excessive Agency (HITL #6 lands W4 Wed; framing context): source linked from `pre-session/D4.md`
- The full HITL programme thread (7 touchpoints): W1 Fri, **W2 Thu = today**, W3 Mon, W3 Wed, W3 Thu, W4 Wed, W5 Wed
- Tomorrow's prep: read `pre-session/D5.md` (RAG eval dimensions + W3 Mon Plan Day preview); Fri AM is eval-harness-build, Fri PM is **first Live Defense of the programme + first standard two-tier MCQ**.
